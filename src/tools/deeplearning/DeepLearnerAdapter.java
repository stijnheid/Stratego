package tools.deeplearning;

import Game.GameBoard;
import Game.Team;

/**
 * This class translates the output of the simulated matches to fit the input
 * required for the neural network training.
 */
public class DeepLearnerAdapter implements DeepLearner {

    @Override
    public void learn(GameBoard initialSetup, Team winner, boolean isAttacker) {

    }
}
